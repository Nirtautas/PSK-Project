
type Props = {
    boardId: number
}

const BoardPage = ({ boardId }: Props) => {
    return (
        <div>
            <h1>BoardPage {boardId}</h1>
        </div>
    )
}

export default BoardPage
